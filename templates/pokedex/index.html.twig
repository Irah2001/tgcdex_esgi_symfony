{% extends 'base.html.twig' %}

{% block title %}Pokédex
{% endblock %}

{% block body %}
	<div class="container mx-auto p-4">
        <div class="text-center mb-12">
            <h1 class="text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-red-500">
                Pokédex
            </h1>
            <p class="text-xl text-gray-600 mt-2">Découvre tous les Pokémon que tu possèdes et ceux qui te manquent !</p>
        </div>

		<div class="mb-8">
			<h2 class="text-3xl font-bold text-gray-800">Pokémon possédés</h2>
			{% if ownedCards|length > 0 %}
				<ul class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-6 mt-4">
					{% for card in ownedCards %}
						<li class="text-center transition-transform transform hover:scale-105 hover:shadow-lg rounded-lg bg-white p-4 border border-gray-200">
							<img src="{{ card.imgUrl }}" alt="{{ card.name }}" class="mx-auto mb-2 w-full h-48 object-contain transition-all duration-300 hover:opacity-80">
							<strong class="block text-sm font-medium text-gray-700">{{ card.name }}</strong>
						</li>
					{% endfor %}
				</ul>
			{% else %}
				<p class="text-center text-gray-600 mt-4">Aucun Pokémon possédé.</p>
			{% endif %}
		</div>

		<div>
			<h2 class="text-3xl font-bold text-gray-800">Pokémon non possédés</h2>
			{% if notOwnedCards|length > 0 %}
				<ul class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-6 mt-4">
					{% for card in notOwnedCards %}
						<li class="text-center transition-transform transform hover:scale-105 hover:shadow-lg rounded-lg bg-white p-4 border border-gray-200">
							<img src="{{ card.imgUrl }}" alt="{{ card.name }}" class="mx-auto mb-2 w-full h-48 object-contain transition-all duration-300 hover:opacity-80">
							<strong class="block text-sm font-medium text-gray-700">{{ card.name }}</strong>
						</li>
					{% endfor %}
				</ul>
			{% else %}
				<p class="text-center text-gray-600 mt-4">Tu possèdes déjà tous les Pokémon ! 🎉</p>
			{% endif %}
		</div>
	</div>
{% endblock %}

